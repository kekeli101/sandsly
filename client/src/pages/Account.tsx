import { useEffect, useState } from "react";
import { ArrowRight, BarChart3, Bike, ChefHat, CheckCircle2, CreditCard, LogIn, LogOut, MapPin, PackageCheck, Save, UserRound } from "lucide-react";
import { useLocation } from "wouter";
import { toast } from "sonner";
import { useAuth } from "@/_core/hooks/useAuth";
import { formatGhsPesewas } from "@/lib/catalog-types";
import { trpc } from "@/lib/trpc";
import { getKitchenProfileAccess } from "@/lib/kitchen-access";

function labelize(value: string) { return value.replaceAll("_", " "); }
const timeline = ["pending", "accepted", "preparing", "ready", "out_for_delivery", "delivered", "completed"];

export default function Account() {
  const { user, isAuthenticated, logout } = useAuth();
  const [, setLocation] = useLocation();
  const utils = trpc.useUtils();
  const profileQuery = trpc.storefront.profile.useQuery(undefined, { enabled: isAuthenticated, retry: false });
  const ordersQuery = trpc.storefront.orders.useQuery(undefined, { enabled: isAuthenticated, retry: false, refetchInterval: isAuthenticated ? 15_000 : false, refetchOnWindowFocus: true });
  const [authMode, setAuthMode] = useState<"login" | "register">("login");
  const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState("");
  const [phone, setPhone] = useState(""); const [defaultAddress, setDefaultAddress] = useState("");
  const kitchenAccess = getKitchenProfileAccess(user?.role);
  const completeAuth = async () => { await utils.auth.me.invalidate(); toast.success(authMode === "register" ? "Account created" : "Signed in"); };
  const loginMutation = trpc.auth.login.useMutation({ onSuccess: completeAuth, onError: (error) => toast.error("Sign-in failed", { description: error.message }) });
  const registerMutation = trpc.auth.register.useMutation({ onSuccess: completeAuth, onError: (error) => toast.error("Registration failed", { description: error.message }) });
  const saveMutation = trpc.storefront.saveProfile.useMutation({ onSuccess: async () => { await utils.storefront.profile.invalidate(); toast.success("Profile saved"); }, onError: (error) => toast.error("Couldn’t save profile", { description: error.message }) });
  useEffect(() => { setPhone(profileQuery.data?.phone ?? ""); setDefaultAddress(profileQuery.data?.defaultAddress ?? ""); }, [profileQuery.data]);
  if (!isAuthenticated) return <AuthScreen authMode={authMode} setAuthMode={setAuthMode} name={name} setName={setName} email={email} setEmail={setEmail} password={password} setPassword={setPassword} pending={loginMutation.isPending || registerMutation.isPending} submit={() => authMode === "register" ? registerMutation.mutate({ name, email, password }) : loginMutation.mutate({ email, password })} />;
  return <div className="min-h-screen bg-[#0b0b0b] px-5 py-10 pb-28 text-[#fff7f2] sm:px-8 md:px-12 md:py-14"><div className="mx-auto max-w-[780px]"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] font-black uppercase tracking-[0.18em] text-[#ff5a1f]">Your account</p><h1 className="mt-2 font-display text-[clamp(2.4rem,10vw,4rem)] font-black uppercase leading-none tracking-[-0.08em]">{user?.name || "Crunch crew"}</h1><p className="mt-3 text-sm text-[#bdb2ac]">{user?.email || "Signed in customer"}</p></div><button type="button" onClick={() => logout().then(() => toast.success("Signed out"))} className="flex h-10 items-center gap-2 rounded-[10px] border border-[#4a403c] px-3 text-[10px] font-black uppercase tracking-[0.12em] text-[#cfc2bb] hover:border-[#ff5a1f] hover:text-[#ff5a1f]"><LogOut size={15} /> Sign out</button></div>{kitchenAccess.canUseKitchen && <section className="mt-8 rounded-[15px] border border-[#48413e] bg-[#242424] p-5 shadow-[3px_3px_0_#050505]"><div className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-[10px] bg-[#ff5a1f] text-[#111111]"><ChefHat size={19} /></span><div className="min-w-0 flex-1"><p className="text-[10px] font-black uppercase tracking-[0.15em] text-[#ff5a1f]">Staff tools</p><h2 className="mt-1 font-display text-[1.2rem] font-black uppercase tracking-[-0.04em]">Restaurant operations</h2><p className="mt-2 text-sm leading-6 text-[#bdb2ac]">{kitchenAccess.description}</p><div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={() => setLocation("/kitchen")} className="inline-flex h-10 items-center gap-2 rounded-[9px] bg-[#ff5a1f] px-4 text-[10px] font-black uppercase tracking-[0.13em] text-[#17100d] shadow-[3px_3px_0_#080808]"><ChefHat size={15} /> Kitchen board <ArrowRight size={15} /></button>{user?.role === "admin" && <button type="button" onClick={() => setLocation("/admin")} className="inline-flex h-10 items-center gap-2 rounded-[9px] border border-[#ff5a1f]/55 px-4 text-[10px] font-black uppercase tracking-[0.13em] text-[#ffb09a]"><BarChart3 size={15} /> Admin dashboard</button>}</div></div></div></section>}<section className="mt-8 rounded-[15px] border border-[#48413e] bg-[#242424] p-5 shadow-[3px_3px_0_#050505]"><div className="flex items-center gap-2"><MapPin size={17} className="text-[#ff5a1f]" /><h2 className="font-display text-[1.1rem] font-black uppercase tracking-[-0.03em]">Delivery details</h2></div><div className="mt-5 grid gap-4"><Label label="Phone"><input value={phone} onChange={(event) => setPhone(event.target.value)} placeholder="e.g. 024 000 0000" /></Label><Label label="Default delivery address"><textarea value={defaultAddress} onChange={(event) => setDefaultAddress(event.target.value)} placeholder="Add your delivery address" rows={3} /></Label><button type="button" disabled={saveMutation.isPending} onClick={() => saveMutation.mutate({ phone, defaultAddress })} className="flex h-11 items-center justify-center gap-2 rounded-[10px] bg-[#ff5a1f] text-xs font-black uppercase tracking-[0.14em] text-[#111111] shadow-[3px_3px_0_#050505] disabled:opacity-60"><Save size={16} /> {saveMutation.isPending ? "Saving…" : "Save details"}</button></div></section><section className="mt-8"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2"><PackageCheck size={17} className="text-[#ff5a1f]" /><h2 className="font-display text-[1.15rem] font-black uppercase tracking-[-0.03em]">Order tracking</h2></div><span className="text-[9px] font-black uppercase tracking-[0.12em] text-[#8e827b]">Auto refreshes</span></div>{ordersQuery.isLoading ? <div className="mt-4 h-28 animate-pulse rounded-[15px] bg-[#242424]" /> : (ordersQuery.data?.length ?? 0) === 0 ? <div className="mt-4 rounded-[15px] border border-[#393432] bg-[#242424] p-6 text-sm leading-6 text-[#bdb2ac]">Your orders and live preparation updates will appear here.</div> : <div className="mt-4 space-y-4">{ordersQuery.data?.map((order) => <OrderCard key={order.id} order={order} />)}</div>}</section></div></div>;
}
function AuthScreen({ authMode, setAuthMode, name, setName, email, setEmail, password, setPassword, pending, submit }: { authMode: "login" | "register"; setAuthMode: (mode: "login" | "register") => void; name: string; setName: (value: string) => void; email: string; setEmail: (value: string) => void; password: string; setPassword: (value: string) => void; pending: boolean; submit: () => void }) { return <div className="min-h-[calc(100vh-62px)] bg-[#0b0b0b] px-5 py-14 text-[#fff7f2] sm:px-8 md:min-h-screen md:px-12 md:py-24"><div className="mx-auto max-w-[620px]"><span className="flex h-14 w-14 items-center justify-center rounded-[14px] bg-[#ff5a1f] text-[#111111] shadow-[4px_4px_0_#000]"><UserRound size={25} /></span><p className="mt-10 text-[10px] font-black uppercase tracking-[0.18em] text-[#ff5a1f]">Your Sandsly account</p><h1 className="mt-2 font-display text-[clamp(2.6rem,12vw,5rem)] font-black uppercase leading-[0.86] tracking-[-0.08em]">Order with confidence.</h1><p className="mt-6 max-w-[430px] text-[0.98rem] leading-7 text-[#bdb2ac]">Save your bag, delivery details, payment selection, and every order milestone.</p><form onSubmit={(event) => { event.preventDefault(); submit(); }} className="mt-8 max-w-[420px] rounded-[15px] border border-[#48413e] bg-[#242424] p-5 shadow-[3px_3px_0_#050505]"><div className="flex gap-2 rounded-[9px] bg-[#171717] p-1"><button type="button" onClick={() => setAuthMode("login")} className={`flex-1 rounded-[7px] py-2 text-[10px] font-black uppercase tracking-[0.12em] ${authMode === "login" ? "bg-[#ff5a1f] text-[#17100d]" : "text-[#a89d96]"}`}>Sign in</button><button type="button" onClick={() => setAuthMode("register")} className={`flex-1 rounded-[7px] py-2 text-[10px] font-black uppercase tracking-[0.12em] ${authMode === "register" ? "bg-[#ff5a1f] text-[#17100d]" : "text-[#a89d96]"}`}>Create account</button></div>{authMode === "register" && <Label label="Name"><input required value={name} onChange={(event) => setName(event.target.value)} /></Label>}<Label label="Email"><input required value={email} onChange={(event) => setEmail(event.target.value)} type="email" /></Label><Label label="Password"><input required minLength={8} value={password} onChange={(event) => setPassword(event.target.value)} type="password" /></Label><button type="submit" disabled={pending} className="mt-5 inline-flex h-12 items-center gap-3 rounded-[10px] bg-[#ff5a1f] px-5 text-xs font-black uppercase tracking-[0.16em] text-[#111111] shadow-[4px_4px_0_#000] disabled:opacity-60"><LogIn size={17} /> {pending ? "Working…" : authMode === "register" ? "Create account" : "Sign in"}</button></form></div></div>; }
function OrderCard({ order }: { order: any }) { const isDelivery = order.orderType === "delivery"; const steps = timeline.filter((step) => isDelivery || !["out_for_delivery", "delivered"].includes(step)); const reached = new Set(order.history?.map((event: any) => event.nextStatus) ?? [order.status]); const currentIndex = steps.indexOf(order.status); return <article className="rounded-[15px] border border-[#48413e] bg-[#242424] p-5 shadow-[3px_3px_0_#050505]"><div className="flex items-start justify-between gap-4"><div><p className="font-display text-lg font-black">{order.orderNumber}</p><p className="mt-1 text-xs text-[#bdb2b2]">{new Date(order.createdAt).toLocaleString("en-GH", { day: "numeric", month: "short", hour: "2-digit", minute: "2-digit" })}</p></div><div className="text-right"><p className="text-[10px] font-black uppercase tracking-[0.12em] text-[#ff5a1f]">{labelize(order.status)}</p><p className="mt-1 font-display font-black">{formatGhsPesewas(order.totalPesewas)}</p></div></div><div className="mt-4 flex flex-wrap gap-2 text-[9px] font-black uppercase tracking-[0.1em]"><span className="inline-flex items-center gap-1.5 rounded-full bg-[#191817] px-2.5 py-1.5 text-[#ded3cc]">{isDelivery ? <Bike size={12} /> : <PackageCheck size={12} />}{order.orderType}</span><span className="inline-flex items-center gap-1.5 rounded-full bg-[#191817] px-2.5 py-1.5 text-[#ded3cc]"><CreditCard size={12} />{labelize(order.paymentMethod)} · {order.paymentStatus}</span></div><div className="mt-5 grid grid-cols-2 gap-2 sm:grid-cols-4">{steps.map((step, index) => { const active = reached.has(step) || index <= currentIndex; return <div key={step} className={`rounded-[8px] px-2 py-2 text-center text-[8px] font-black uppercase tracking-[0.08em] ${active ? "bg-[#ff5a1f]/15 text-[#ffb09a]" : "bg-[#191817] text-[#706660]"}`}>{active && <CheckCircle2 className="mx-auto mb-1" size={12} />}{labelize(step)}</div>; })}</div><div className="mt-4 border-t border-[#433b37] pt-4 text-sm"><p className="text-[10px] font-black uppercase tracking-[0.12em] text-[#a89d96]">Items</p><ul className="mt-2 space-y-1.5 text-[#e9ded8]">{order.items.map((item: any) => <li key={`${order.id}-${item.productName}`} className="flex justify-between"><span>{item.productName} ×{item.quantity}</span><span>{formatGhsPesewas(item.lineTotalPesewas)}</span></li>)}</ul>{isDelivery && <p className="mt-3 rounded-[8px] bg-[#191817] px-3 py-2 text-xs leading-5 text-[#d8cbc4]"><MapPin className="mr-1 inline text-[#ff5a1f]" size={13} />{order.deliveryAddress}</p>}</div></article>; }
function Label({ label, children }: { label: string; children: React.ReactNode }) { return <label className="mt-4 block text-[10px] font-black uppercase tracking-[0.13em] text-[#bdb2ac]">{label}<span className="mt-2 block [&_input]:h-11 [&_input]:w-full [&_input]:rounded-[9px] [&_input]:border [&_input]:border-[#4a403c] [&_input]:bg-[#171717] [&_input]:px-3 [&_input]:text-sm [&_input]:font-normal [&_input]:normal-case [&_input]:tracking-normal [&_input]:text-[#fff7f2] [&_textarea]:w-full [&_textarea]:rounded-[9px] [&_textarea]:border [&_textarea]:border-[#4a403c] [&_textarea]:bg-[#171717] [&_textarea]:px-3 [&_textarea]:py-2.5 [&_textarea]:text-sm [&_textarea]:font-normal [&_textarea]:normal-case [&_textarea]:tracking-normal [&_textarea]:text-[#fff7f2]">{children}</span></label>; }
